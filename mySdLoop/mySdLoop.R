#    Compute the stadard deviation of numeric vector v using loops. 
#    If there are fewer than two elements in v return NA.
#    Do not use any built-in R functions.

mySdLoop <- function(v) {
  stopifnot(is.vector(v) && is.numeric(v))

}