#   numOver.R
#
#   function numOver(v, loop=true) returns the number of even elements in numeric vector v.
#   If loop is true, use a loop. Otherwise do not use any loops in your solution.

numEven <- function(v,  loop=TRUE) {

}