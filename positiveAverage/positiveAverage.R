#    Write function positiveAverage(v) that returns the average
#    of only the positive, non-zero values in numeric vector v.
#    Do not use a loop. Assume that v has at least one element.
#    Return NA if v has no positive values.

positiveAverage <- function(v) {
  stopifnot(is.vector(v))
  
}
