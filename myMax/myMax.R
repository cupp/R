#   myMax.R
#
#   function myMax(v) returns the largest elements of numeric vector, v.
#   Use a loop. You are not allowed to use the max function in your solution.


myMax <- function(v) {
  assert(length(v) > 0)
  
}