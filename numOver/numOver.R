#   numOver.R
#
#   function numOver(v, x, loop=TRUE) returns the number of elements in numeric vector v that are larger than numeric x.
#   If loop is true, use a loop. Otherwise do not use any loops in your solution.

numOver <- function(v, x, loop=TRUE) {

}